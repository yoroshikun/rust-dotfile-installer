use std::process::Command;

// TODO proper return types
fn brew_install() -> () {
  // TODO runs brew installation
}
// TODO proper return types
fn brew_tap() -> () {
  // TODO runs brew tap
}
// TODO proper return types
fn brew_cask_install() -> () {
  // TODO runs brew cask installations
}

fn brew_readfile() -> {
  // TODO Reads the brew package list and parses it
}

fn brew_cask_readfile() -> {
  // TODO Reads the brew package list and parses it
}